import React from 'react';
import ReactDOM from 'react-dom/client';
import CTFScraper from './Code';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CTFScraper />
  </React.StrictMode>
);
